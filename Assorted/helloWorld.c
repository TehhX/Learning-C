// Standard IO library
#include <stdio.h>

// Enter point function signature
int main() {
    // Print to console
    printf("Hello, world.\n");

    // Return exit code (will return 0 if none specified)
    return 0;
}
